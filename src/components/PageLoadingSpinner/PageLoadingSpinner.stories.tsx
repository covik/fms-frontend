import { Meta } from '@storybook/react';
import { PageLoadingSpinner } from './PageLoadingSpinner';

export default {
  component: PageLoadingSpinner,
} satisfies Meta<typeof PageLoadingSpinner>;

export const Default = {};
